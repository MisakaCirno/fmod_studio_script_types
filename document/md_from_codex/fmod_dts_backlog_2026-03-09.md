# FMOD DTS Backlog (2026-03-09)

## 背景
- 以下事项尚未完成。
- 已从 `npm run build:dts` 的控制台 TODO 提示中移除，避免每次构建刷屏。
- 改为集中记录在本文件，避免遗漏。

## 未完成事项
1. 返回类型推断增强（高优先级）
- 目标：从 `lookup/find/owner/member-access` 这类返回路径中继续推断更精确的返回实体类型。
- 当前状态：未完成。
- 影响：部分 extension 方法仍为 `any` 或宽泛联合类型。

2. runtime instance augmentation 启发式校验（高优先级）
- 目标：用更多真实项目快照验证并收紧 runtime 实例接口增强规则，减少误判/过宽。
- 当前状态：未完成。
- 影响：个别实体的实例增强可能偏宽松。

## 执行建议
1. 每完成一项后，同步更新本文件状态与日期。
2. 若新增未完成项，统一追加到本文件，不再放入构建日志输出。
